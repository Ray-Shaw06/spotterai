# SpotterAI Release 1 Worklog

This document is the version-controlled source of truth for Release 1 progress, decisions, verification evidence, deployment state, blockers, and remaining work. It must not contain secrets, push endpoints, personal data, submitted health information, photographs, or raw AI content.

## Release baseline

| Item | Value |
| --- | --- |
| Production baseline | `1b94973` (`origin/main`) |
| Existing deployment | Vercel |
| Existing sync | Firebase Auth and Firestore |
| Release branch | `codex/release-1`, created from the approved Release 1 baseline |
| Design | `docs/superpowers/specs/2026-07-17-release-1-design.md` |
| Implementation plan | `docs/superpowers/plans/2026-07-17-release-1.md` |
| Current phase | Tasks 1–7 plus the Task 8 local, application-preview, and repository-owned Firestore emulator phases are complete. The replacement preview for exact commit `bfacd17` verified the core Release 1 flow, successful meal-photo estimation, and safe non-food recovery with clean browser/runtime evidence. Task 8 WAF, Blaze/budget approval, production secrets/deployment, installed-device notification delivery, and production phases remain pending. |

## Gate status

| Gate | Status | Evidence or next action |
| --- | --- | --- |
| Product scope approved in conversation | Complete | Scope and constraints captured in the Release 1 design |
| Written design approved | Complete | Owner approved the written specification on 2026-07-17 |
| Implementation plan approved | Complete | Plan derives directly from the approved design and the owner selected autonomous agent execution |
| Clean release worktree | Complete | Release work proceeds on isolated `codex/release-1`; unrelated local work is excluded |
| Measurements and conversions | Complete | Task 1 committed: metric/imperial onboarding conversions, validation, and accessibility checks |
| First-workout activation | Complete | Task 2 committed: plan day-one action and bounded activation funnel behavior |
| Funnel analytics | Complete | Task 2 committed: privacy-safe allow-listed Vercel virtual pageviews |
| AI retry and recovery states | Complete | Task 3 committed: bounded client timeouts, safe recovery copy, saved-plan retry, and in-memory photo retry |
| Web Push client and preferences | Complete locally | Task 4 defines the privacy-safe preference domain; Task 7 adds installed-PWA capability checks, explicit opt-in, Account controls, service-worker receipt/click routing, and completion-date-only sync. Physical-device and deployment evidence remain pending. |
| Notification API and dispatcher | Complete locally | Task 5 provides the default-off anonymous API/storage boundary; Task 6 adds the tested scheduler, lease dispatcher, bounded cleanup, strict push-provider allow-list, and server-only Firestore rules. Production remains owner-gated. |
| Independent reviews resolved | Complete locally | Mobile/PWA, security/privacy, and specification final reviews approved the pre-preview tree with zero Critical or Important findings. The narrow post-preview meal-photo validation fix is independently Approved with zero Critical, Important, or Minor findings. One known deferred Minor covers only externally/Admin-corrupted parked data missing `updatedAt`; normal writes are unaffected. |
| Full automated verification | Complete locally | Current full suite passes 427/427; the exact release-focused suite passes 264/264; evaluator expectations pass 23/23 with 13/13 risky plans caught and zero false positives; nutrition anchors pass 15/15. |
| iPhone and Android installed-PWA checks | Pending | Requires owner-controlled physical devices |
| Vercel preview verification | Complete at the application layer | Replacement deployment `dpl_AxgjNKmvxNtZu7pp5dCfsFYCGtUK` for exact commit `bfacd177e2fcb7fdcb8f278ba039e7350b430d4f` reached READY. Live checks covered the shell/PWA assets, default-off notification API, plan/onboarding/Coach/workout flow, successful lamb-chops meal estimation, and the non-food retry/manual-entry recovery. Browser logs and Vercel warning/error/fatal plus 5xx runtime queries were empty. This does not satisfy the separate Firebase, WAF, or installed-device gates. |
| Firebase emulator or controlled-project verification | Complete locally | A fixed `demo-*` project ran the checked-in rules plus the real API/store and dispatcher against Firestore Emulator v1.21.0: 5/5 checks passed with no external project, secret, or push access. Production rules/index deployment remains owner-gated. |
| Production notification configuration | Blocked by owner gate | Requires explicit approval of Firebase Blaze billing and production secrets |
| Production promotion and smoke test | Pending | Requires all prior release gates |

## Decisions

| Date | Decision | Reason |
| --- | --- | --- |
| 2026-07-17 | Keep the current static application, Vercel hosting, Firebase sync, PWA, AI endpoints, and tests. | Release 1 improves the current product without migration risk. |
| 2026-07-17 | Support installed iPhone and Android PWAs with standards-based Web Push. | This gives native-style background notifications while retaining the website/PWA model. |
| 2026-07-17 | Use Firestore plus a Firebase scheduled Cloud Function for notification dispatch. | User-selected reminder times need more frequent and predictable scheduling than Vercel Hobby cron provides. |
| 2026-07-17 | Use an anonymous per-device subscription and minimal operational data. | Notifications must work without Google sign-in and without creating a new health-data store. |
| 2026-07-17 | Ask for notification permission only after plan creation and a deliberate enable action. | This keeps permission contextual and user-controlled. |
| 2026-07-17 | Prefill reminder days from the plan, then require confirmation or editing. | The schedule is useful immediately but remains under user control. |
| 2026-07-17 | Default quiet hours to 10:00 PM–8:00 AM and cap at two notifications per day. | Notifications should be useful without becoming intrusive. |
| 2026-07-17 | Label onboarding systems Metric and Imperial; use cm/kg and ft/in/lb inputs. | Users should never have to infer a measurement unit. |
| 2026-07-17 | Keep height optional and do not claim it affects calculations that do not use it. | The current plan and nutrition paths do not use height. |
| 2026-07-17 | Keep consumer features free in Release 1. | Monetization experiments are not allowed to weaken the core user experience. |
| 2026-07-17 | Represent critical funnel actions as sanitized manual Vercel pageviews under `/funnel/<event>`. | Official Vercel documentation currently limits custom events to Pro/Enterprise, while manual pageviews remain compatible with Hobby and require no new analytics provider. |

## Rejected or deferred alternatives

| Alternative | Outcome | Reason |
| --- | --- | --- |
| Firebase Cloud Messaging browser SDK for all supported devices | Rejected | Standards-based Web Push provides the intended installed-iPhone path without relying on an unsupported assumption. |
| Vercel Hobby cron as the reminder dispatcher | Rejected | Daily, imprecise invocation cannot honor user-selected times. |
| Request notification permission on first visit | Rejected | It lacks context and is likely to reduce trust and opt-in quality. |
| Store full plans or health profiles for notification targeting | Rejected | The Release 1 notification use cases need only schedule and minimal activity state. |
| Guaranteed iOS Live Activities or background rest timers | Deferred to native-app investigation | Web PWAs cannot promise native background execution semantics. |
| Paid consumer features | Deferred | Release 1 keeps the complete consumer product free. |

## Chronological log

### 2026-07-17 — Release definition and design

- Role: Product orchestrator.
- Work: Audited the current architecture and converted the approved Release 1 decisions into a written design.
- Result: Defined measurement UX, first-workout activation, analytics taxonomy, AI recovery states, notification architecture, privacy boundaries, tests, rollout, and release gates.
- Evidence: `docs/superpowers/specs/2026-07-17-release-1-design.md`.
- Production baseline: `1b94973`.
- Implementation status: No Release 1 product code changed during this phase.
- Safety: Unrelated local Paddington commits and the untracked `output/` directory are outside Release 1 and must not be included in its branch or deployment.
- Owner gate: Written design review is required before the implementation plan and agent execution begin.
- External dependency: Production scheduled notifications require owner-approved Firebase Blaze billing and server-side secrets. Neither is authorized or configured by this entry.

### 2026-07-17 — Written design approval and implementation plan

- Role: Product orchestrator.
- Owner decision: Approved the written Release 1 design.
- Work: Mapped the approved design into eight testable implementation tasks with a fresh implementation agent and independent reviewer gates for each task.
- Result: `docs/superpowers/plans/2026-07-17-release-1.md` defines exact files, interfaces, tests, commits, privacy boundaries, preview checks, billing/secret gates, real-device checks, and production sign-off.
- Evidence: Current codebase inspection, existing tests and interfaces, and official Vercel analytics documentation current on 2026-07-17.
- Compatibility decision: Use manual Vercel funnel pageviews because Vercel Hobby supports pageviews but not custom events; no new analytics vendor or user identifier is introduced.
- Safety: Implementation will start from production baseline `1b94973` in an isolated worktree and will cherry-pick only Release 1 documentation.
- External gate unchanged: No Firebase Blaze billing, production secrets, Firebase deployment, preview deployment, or production promotion has been authorized by this entry.

### 2026-07-17 — Task 1: metric and imperial onboarding measurements

- Role: Implementation agent.
- Bounded task: Add metric/imperial onboarding measurement conversion, validation, and accessible unit-specific inputs while preserving the `spotterai_onboarding` saved-data key, legacy `units: "kg" | "lb"`, and kilogram nutrition inputs.
- Result: Added `measurements.js` with the specified conversion constants and pure interfaces. Metric uses cm/kg; imperial uses ft/in/lb. Height and weight remain optional, but any supplied value must be in the declared range. Nutrition receives kilograms in both systems.
- Conversion boundaries: metric height is 100–250 cm and weight is 30–350 kg; imperial total height is 3 ft 3 in–8 ft 2 in, inches are 0–11, and weight is 66–772 lb. Switching converts 178 cm/75 kg to 5 ft 10 in/~165.3 lb and back within the tested rounding tolerance.
- Accessibility: measurement inputs have explicit aria-labels, decimal/numeric input modes, live `aria-invalid` state, and error text via `aria-describedby`; visible unit labels are cm, kg, ft, in, and lb. Invalid optional values disable both Next and Skip so they cannot seed nutrition targets.
- Files: `measurements.js`, `onboarding.js`, `onboarding-ui.js`, `style.css`, `test/measurements.test.js`, `test/onboarding.test.js`, `test/ui-copy.test.js`.
- TDD evidence: initial `node --test test/measurements.test.js test/onboarding.test.js` failed with expected `ERR_MODULE_NOT_FOUND` for `measurements.js`; regression test for invalid-value Skip behavior then failed before its UI guard was added. Both GREEN runs passed.
- Verification: `node --test test/measurements.test.js test/onboarding.test.js test/ui-copy.test.js` — 22/22 passed. `npm test` — 204/204 passed.
- Independent review: found a P1 Skip bypass for invalid optional values and a missing worklog entry. Resolved P1 by disabling Skip whenever `canAdvance()` is false and added a regression test; this entry resolves the documentation finding. No outstanding review findings.
- Commit: `feat: clarify onboarding measurements`.

### 2026-07-17 — Task 2: Hobby-compatible funnel analytics and first-workout activation

- Role: Implementation agent.
- Bounded task and acceptance criteria: Add privacy-safe activation-funnel analytics compatible with Vercel Hobby, make the plan's first workout immediately actionable, and preserve existing Today and Dashboard quick-start behavior.
- Result: Added `analytics.js` with an immutable allow-list schema and `trackFunnel(name, properties?)`. It uses only sanitized manual virtual pageviews (`window.va("pageview", { route, path })`): no user IDs, intake values, prompts, measurements, photos, health notes, or arbitrary properties can become telemetry. The plan results now have one primary "Start my first workout" action for day one. Workout sessions retain an in-memory source, record their start once, and record completion only after `addWorkout` returns a workout.
- Outcome boundaries: Generation records success only after a plan is published and rendered, records a fallback only after its saved plan is displayed, and records failure only after both live generation and fallback are unavailable. Onboarding, landing CTAs, and meal-photo result paths are instrumented without intake or image data.
- Compatibility decision: Sanitized virtual pageviews were selected because current official Vercel documentation limits custom events to Pro/Enterprise while pageviews remain available on Hobby. The existing first-party Vercel Analytics loader remains unchanged.
- Files: `analytics.js`, `index.html`, `app.js`, `onboarding-ui.js`, `workout-ui.js`, `nutrition-ui.js`, `style.css`, `test/analytics.test.js`, `docs/release-1-worklog.md`.
- TDD evidence: `node --test test/analytics.test.js test/ui-copy.test.js test/ui-layout.test.js` initially failed as expected with `ERR_MODULE_NOT_FOUND` for `analytics.js`. Follow-up RED/GREEN tests independently proved nested schema mutation was blocked, editing a historical workout does not emit an activation start, and a null generation response is classified as `invalid_response`.
- Verification: `node --test test/analytics.test.js test/ui-copy.test.js test/ui-layout.test.js test/today.test.js` passed 34/34. `npm test` passed 210/210. `git diff --check` passed with no whitespace errors.
- Independent review: Found and resolved two important issues before commit: shallow freezing allowed runtime mutation of enum values, and historical workout edits emitted unpaired start events. Also hardened null JSON response classification. The final re-review approved the diff with no blockers; the remaining caveat is that lifecycle coverage is source-shape rather than browser interaction coverage.
- Commit: `feat: instrument the Release 1 activation funnel`.

### 2026-07-17 — Task 3: actionable AI timeout, fallback, and retry states

- Role: Implementation agent.
- Bounded task and acceptance criteria: Add client-side 65-second plan and 35-second nutrition/photo request deadlines; map failures only to the approved safe enum; preserve saved-plan audit and editable manual nutrition fallback; make plan and photo recovery actionable without exposing provider errors.
- Result: Added `ai-errors.js` with abort-signal composition, timeout translation, safe classification, and provider-neutral copy. Plan generation keeps `lastInputs`, renders/audits its saved-plan fallback, shows the classified safe reason, and offers “Try live generation again.” Food/manual entry remains editable. A failed meal photo retains only the current `File` object in page memory for “Try this photo again,” and clears it after success, replacement, or closing the detail. Stale photo requests cannot overwrite a newer selection.
- Boundaries preserved: No provider-routing, Gemini/Groq server, Firebase, notification, deployment, or user-data persistence changes. Funnel events retain only the existing classified `failure_class` enum.
- Files: `ai-errors.js`, `ai.js`, `app.js`, `index.html`, `nutrition-ui.js`, `style.css`, `test/ai-errors.test.js`, `test/ui-copy.test.js`, `docs/release-1-worklog.md`.
- TDD evidence: RED 1: `node --test test/ai-errors.test.js test/ui-copy.test.js` failed as expected with `ERR_MODULE_NOT_FOUND` for `ai-errors.js` (and the missing recovery UI assertion). GREEN 1: the required focused suite passed 31/31 after timeout/classification/retry wiring. RED 2: `node --test test/ai-errors.test.js` failed with “Missing expected rejection” for a string-shaped food response. GREEN 2: the same focused suite passed 32/32 after treating non-object estimate shapes as `invalid_response`.
- Verification: `node --test test/ai-errors.test.js test/ui-copy.test.js test/estimate.test.js test/gemini-groq.test.js` — 32/32 passed. `npm test` — 215/215 passed. `git diff --check` passed.
- Self-review: Verified external aborts remain `AbortError`, only the helper’s own timer becomes `TimeoutError`, all displayed recovery text is provider-neutral, fallback is still audited, and a superseded photo request cannot clear or replace the latest retry file.
- Commit: `feat: add recoverable AI failure states`.

### 2026-07-17 — Task 4: notification schedule and preference domain

- Role: Implementation agent.
- Bounded task and acceptance criteria: Add only the pure notification schedule preset, normalization, and validation interfaces. Use exact safe preference fields; validate IANA zones and `HH:mm` times; cap schedules at seven unique ISO weekdays; and retain no profile, plan, nutrition, measurement, injury, or free-text data.
- Result: Added a capability-independent `notifications.js` domain module. It prefills the specified two-to-six-day schedules with local 18:00 reminders, defaults quiet hours to 22:00–08:00, enables all four categories, and leaves notifications unpaused. Normalization returns a fresh allow-listed payload and removes all unknown nested and top-level data. Validation returns a normalized value plus field errors for malformed zones, schedules, quiet hours, categories, or pause state.
- Files: `notifications.js`, `test/notifications.test.js`, `docs/release-1-worklog.md`.
- TDD evidence: `node --test test/notifications.test.js` first failed with the expected `ERR_MODULE_NOT_FOUND` for `notifications.js`. After the minimal implementation, the same focused suite passed 7/7.
- Verification: `npm test` passed 224/224. `git diff --check` passed with no whitespace errors.
- Self-review: The module is pure and imports no platform/client code. Its output contains only timezone, schedule, quiet hours, the four boolean category controls, and paused state; it neither reads nor forwards health, profile, free-text, subscription, or endpoint data. No service worker, Firebase, API, deployment, or UI files changed.
- Initial commit: `b57a1b9` — `feat: define notification schedules and preferences`.
- Formal review findings: The reviewer blocked the initial implementation because `Intl.DateTimeFormat` accepts raw offset identifiers such as `+05:30` and `-04:00`, while this contract requires named IANA zones. The reviewer also required a stable invalid-zone fallback rather than the runtime local zone. Minor hardening identified prototype-supplied nested fields and explicit non-mutation/reference-isolation coverage.
- Resolution: `6c7aabe` — `fix: harden notification timezone validation` rejects raw positive and negative offset identifiers before platform zone validation, uses `UTC` for missing or invalid zones, requires own schedule/category/quiet-hour properties, and preserves fresh nested normalized output.
- Review-fix verification: `node --test test/notifications.test.js` passed 10/10; `npm test` passed 227/227; `git diff --check` passed.
- Re-review: Approved. No blocking findings remain for Task 4.
- Remaining scope: Notification client controls, anonymous API/storage, dispatcher, real-device verification, Firebase configuration, and deployment remain separate tasks and owner-gated where applicable.

### 2026-07-17 — Task 5: anonymous notification API and server-only Firestore adapter

- Role: Implementation agent.
- Bounded task and acceptance criteria: Add only the anonymous Vercel notification registration/update/delete API, HMAC per-device authorization, strict subscription/preference validation, lazy Firebase Admin initialization, and the fixed `notificationDevices` adapter. Client UI, service worker, dispatcher/functions, production deployment, billing, and real secrets remained out of scope.
- Result: Added a 32 KB, no-store, same-origin API with safe GET configuration; server-generated 32-byte device IDs; versioned HMAC-SHA256 tokens with 180-day expiry, five-minute future skew, minimum 32-character secrets, and timing-safe signature comparison; HTTPS Web Push validation; exact normalized preference storage; PATCH recalculation sentinels; per-device DELETE; and generic responses. Structured logs contain only route, method, request ID, status, duration, and safe failure class.
- Storage/privacy boundary: The API reconstructs records and patches from allow-listed values before the mock or Firestore adapter is called. Stored fields contain subscription encryption material, scheduling controls, the minimal completion date/scheduler state, and operational timestamps only—never profile, plan, measurements, nutrition, injuries, free text, or client-selected document IDs. Firebase Admin is initialized lazily from server-side JSON with escaped-newline normalization and existing-app reuse; no browser Firebase configuration is imported.
- Dependency decision: Declared `firebase-admin@^13.4.0`, resolving to `13.10.0`, whose installed package declares Node `>=18`, matching this repository. The dependency is server-only. `npm audit --omit=dev` reports eight moderate transitive findings through `uuid@9.0.1`; npm's only offered complete remediation is a forced breaking downgrade to `firebase-admin@10.3.0`, so no unsafe automated fix was applied.
- Files: `lib/firebase-admin.js`, `lib/notification-auth.js`, `lib/notification-validation.js`, `lib/notification-store.js`, `api/notifications.js`, `package.json`, `package-lock.json`, `.env.example`, `vercel.json`, `test/notification-auth.test.js`, `test/notification-api.test.js`, and `docs/release-1-worklog.md`.
- TDD evidence: Initial focused RED failed 0/2 at module load with the expected `ERR_MODULE_NOT_FOUND` for `lib/notification-auth.js`, both before and after dependency installation. The first implementation GREEN passed 21/21. A configuration test then failed 21/22 because npm had saved `^13.10.0` instead of the approved compatibility floor; after normalizing the manifest/configuration, GREEN passed 22/22. A further hardening RED failed 21/22 because a noncanonical P-256 key reached storage; canonical 65-byte uncompressed P-256 and 16-byte auth validation restored 22/22 GREEN.
- Verification: Focused `node --test test/notification-auth.test.js test/notification-api.test.js` passed 22/22. Full `npm test` passed 249/249. Module syntax checks and `git diff --check` passed. Dependency tree inspection confirmed `firebase-admin@13.10.0` with the audited `uuid@9.0.1` transitive path.
- Security/privacy self-review: No authentication bypass was found; PATCH/DELETE derive the record only from a verified bearer token, so body overposting cannot select another device. Validators rebuild exact stored shapes. GET exposes only the public VAPID key. Client responses and logs exclude secrets, endpoint/key/token/document-ID/date/body values. Token timing, tampering, expiry, and future skew are covered by tests. The adapter imports Admin SDK modules only and hard-codes `notificationDevices`.
- Remaining gates/concerns: Task 6 must add and deploy Firestore rules that deny direct browser access before this collection can be called server-only in production, and must assess arbitrary-HTTPS endpoint abuse/SSRF and anonymous registration rate limiting before dispatch is enabled. No live Firestore call, real push, production secret, billing change, preview deployment, or production deployment occurred. Independent Task 5 review remains pending.
- Commit: `feat: add anonymous notification subscription API`.

### 2026-07-17 — Task 5 formal review fixes

- Role: Implementation agent responding to an independent security review. Scope remained the anonymous notification API and Admin storage adapter; no Task 6 dispatcher, client UI, deployment, billing, production secrets, or live push work was performed.
- Review result: The initial `1330fcb` implementation was rejected. Blocking findings covered default-on/incomplete readiness, no durable global cap or endpoint idempotency, incomplete same-origin enforcement, framework-parsed body reliance, a legacy default export instead of Vercel's Web Handler, weak Firebase/VAPID/token validation, and an unpinned runtime.
- Resolution: Registration now defaults off and can become ready only with canonical 32-byte token/dedup secrets, an on-curve VAPID public key, an exact HTTPS allowed origin, a bounded daily cap, a non-placeholder WAF rule identifier, and a parseable matching Firebase service account with a real RSA private key. The rule identifier is only a code-readiness input: preview must separately prove 429 responses for excess POST registration, authenticated PATCH, and authenticated DELETE before `NOTIFICATION_REGISTRATION_ENABLED=true`. All writes require JSON and the exact origin, reject cross-site Fetch Metadata, emit no CORS headers, and enforce 32 KB while streaming the raw Fetch body before JSON parsing. The production export follows Vercel's documented Node.js Web Handler `{ fetch(request) }` contract; the dependency-injected core remains available for tests.
- Durable abuse/idempotency boundary: `notificationDevices`, `notificationEndpointIndex`, and `notificationRegistrationCounters` are updated in Firestore transactions. A keyed HMAC-SHA256 endpoint fingerprint enables active-device reuse. Every accepted registration write, including refresh, atomically consumes the UTC-day global cap and returns 429 once full. Stored device/index records carry `authorizationExpiresAt`; DELETE removes a matching helper index transactionally.
- Runtime/Admin hardening: Node is pinned to `22.x`. Firebase Admin uses only the dedicated `spotterai-notifications` app and fails if its project identity differs from the configured service account. P-256 subscription and VAPID values must be real points on `prime256v1`. Device IDs, signatures, and both HMAC secrets must be exact canonical unpadded base64url encodings of 32 bytes, including rejection of unused-bit malleability.
- TDD evidence: The review-fix focused RED failed 0/3 on missing fingerprint/cap/fetch-handler interfaces. The implemented focused suite passed 35/35. Full `npm test` passed 262/262 on Node 22.17.0. Module syntax checks and `git diff --check` passed.
- Dependency audit: `npm audit --omit=dev` still reports eight moderate transitive `uuid@9.0.1` findings through Firebase Admin's Google Cloud clients. npm offers only a forced breaking Firebase Admin major change; no forced remediation was applied.
- Remaining gates: `NOTIFICATION_REGISTRATION_ENABLED` must remain `false` until Task 8/production ownership publishes a fixed-window per-IP WAF rule for `POST`, `PATCH`, and `DELETE /api/notifications` and records its real rule ID. Preview must separately prove 429 responses for excess POST registration, authenticated PATCH, and authenticated DELETE before `NOTIFICATION_REGISTRATION_ENABLED=true`. The environment rule ID is only a fail-closed readiness input; it does not prove the edge rule exists. Task 6 must deny direct client access to all three notification collections, handle expiry cleanup for devices and matching endpoint indexes, and complete dispatcher endpoint/SSRF controls. Expired dedup entries intentionally hand off old-record cleanup to Task 6. Production secrets, Firebase billing, preview/live Firestore checks, physical-device checks, and promotion remain owner-gated.
- Re-review: Pending. No production deployment or external state change occurred.

### 2026-07-17 — Task 5 second security re-review fixes

- Role and scope: Implementation agent responding to the second Task 5 security review. Work remained limited to notification registration/API hardening; Task 6 was not started.
- Review result: `56c3b32` was rejected because active endpoint refresh returned before the daily counter read and merged a complete new-registration record into the existing device. Repeated POSTs could bypass the durable bound and overwrite completion, delivery, sent-category, lease, creation, revocation, and future dispatcher-owned state. Additional findings required fragment rejection, WAF coverage for every mutating method, and HMAC key separation.
- Resolution: Every accepted new, expired, or active registration now reads/checks and increments the same UTC-day transaction counter. Cap exhaustion happens before any device/index/counter mutation. Active refresh updates only endpoint/key/expiration material, normalized preferences, authorization expiry, the recalculation sentinel, and update timestamps; all dispatcher-owned and unknown existing fields are preserved. Indexed records whose `enabled` state is not exactly `true` fail closed with a generic 409 and cannot be replaced or re-enabled; DELETE remains the clean re-registration path.
- Validation/edge hardening: Raw `#` fragments are rejected before URL normalization or endpoint fingerprinting, so `#one`/`#two` aliases cannot create separate registrations. Enabled readiness rejects identical token/dedup secrets. The required owner-controlled fixed-window per-IP WAF rule covers `POST`, `PATCH`, and `DELETE /api/notifications`. Preview must separately prove 429 responses for excess POST registration, authenticated PATCH, and authenticated DELETE before `NOTIFICATION_REGISTRATION_ENABLED=true`. Merely configuring a rule ID is not publication evidence.
- TDD evidence: The second-review focused RED passed 33/40 and failed the seven intended assertions: identical-secret readiness, generic revoked response, fragment alias rejection, refresh field preservation, active-refresh cap enforcement, repeated-refresh bounding, and disabled-record fail-closed behavior. Focused GREEN passed 40/40.
- Verification and audit: Focused notification tests passed 40/40; full `npm test` passed 267/267 on Node 22.17.0; syntax checks and `git diff --check` passed. `npm audit --omit=dev` remains at eight moderate transitive `uuid@9.0.1` findings; the offered complete remediation is a forced breaking Firebase Admin change, so none was applied.
- Commit/re-review: Commit message `fix: bound notification registration refreshes`; independent re-review remains pending. No deployment, billing, secret, or external edge change occurred.

### 2026-07-17 — Task 5 abuse-control gate copy completion

- Final review finding: Deployment-gate copy covered all mutating methods but required explicit preview 429 evidence only for registration and authenticated PATCH, omitting authenticated DELETE.
- Resolution: Every versioned owner/WAF gate statement now requires a published fixed-window per-IP rule for `POST`, `PATCH`, and `DELETE /api/notifications`, plus separate preview 429 evidence for excess POST registration, authenticated PATCH, and authenticated DELETE before `NOTIFICATION_REGISTRATION_ENABLED=true`. A focused regression assertion protects the versioned environment and worklog copy.
- TDD evidence: The focused API copy assertion passed 22/23 and failed on the missing three-method evidence sentence before documentation changes, then passed 23/23 after the copy fix.
- Verification/commit: Focused notification tests passed 40/40; full `npm test` passed 267/267; `git diff --check` passed. Commit message: `docs: complete notification abuse-control gate`. No production code or Task 6 file changed.

### 2026-07-17 — Task 6: privacy-safe scheduled Web Push dispatcher

- Role and bounded scope: Task 6 implementation agent. Added only the Node 22 Firebase scheduled Functions package, pure local-time scheduler, dependency-injected dispatcher, Firebase rules/index/configuration, deterministic tests, and this evidence. Client UI, service worker, billing, secrets, WAF, deployment, live Firestore, and real push remained out of scope.
- Result: `nextNotification` calculates confirmed workout, two-hour follow-up or streak, and next-morning recovery candidates with Luxon; applies category controls, completion suppression, category/local-date deduplication, cross-midnight quiet hours, DST behavior, and the two-per-local-day cap; and emits only the fixed gentle six-field branded payload. `dispatchDue` queries at most 200 due devices, recalculates sentinels, claims a ten-minute transactional lease, finalizes 201/204, deletes terminal subscriptions, quarantines malformed targets, and releases retryable failures without advancing.
- Authorization and cleanup: Expired authorization/device cleanup, old registration-counter cleanup, and orphan/expired endpoint-index review use bounded queries of 50. Device deletion removes the endpoint index only when its current `deviceId` still matches; tests preserve a newer mapping. Healthy index reviews rotate forward so an old orphan cannot remain hidden behind the same first 50 rows. Daily-cap records defer to the next local midnight rather than starving the due query.
- SSRF/redirect boundary: Only exact HTTPS endpoints on `web.push.apple.com`, `fcm.googleapis.com`, or `updates.push.services.mozilla.com` can reach the pinned sender. IP literals, loopback/RFC1918/link-local/internal/unsupported hosts, credentials, fragments, non-default ports, suffix tricks, encoded raw authorities, and non-HTTPS endpoints make zero outbound calls and are quarantined safely. The pinned `web-push` implementation uses direct `https.request` without redirect following; 3xx is treated as terminal invalidation.
- Firestore and secrets: Existing `users/{uid}` access remains authenticated owner-only. Direct client reads/writes to `notificationDevices`, `notificationEndpointIndex`, and `notificationRegistrationCounters`, plus every unmatched path, are denied. The `enabled ASC, nextNotificationAt ASC` composite index is declared. `dispatchNotifications` runs every five minutes in `us-central1` with UTC scheduler time and Firebase secret parameters; no value is committed.
- Files: `functions/package.json`, `functions/package-lock.json`, `functions/index.js`, `functions/notification-schedule.js`, `functions/dispatcher.js`, `firebase.json`, `firestore.rules`, `firestore.indexes.json`, `test/notification-schedule.test.js`, `test/notification-dispatcher.test.js`, and `docs/release-1-worklog.md`.
- TDD evidence: Initial and post-install focused RED failed 0/2 on the two missing production modules. Scheduler GREEN passed 11/11 after isolating the streak fixture from the earlier recovery candidate. Dispatcher behavior GREEN passed 13/13 before configuration; configuration/export additions produced 26/26. Separate fairness RED/GREEN cycles fixed daily-cap due-query starvation and bounded orphan-index starvation. Final focused GREEN passed 28/28 without a real push, network request, or Firestore call.
- Verification: Baseline `npm test` passed 267/267. Final focused tests passed 28/28. Final full `npm test` passed 295/295. All three production Function files passed `node --check`; the isolated direct dependency tree is valid; resolved versions are Firebase Admin 14.2.0, Firebase Functions 7.3.0, Luxon 3.7.2, and web-push 3.6.7.
- Audit: Root production audit reports eight moderate and no high/critical findings; Functions production audit reports seven moderate and no high/critical findings. Both are the known transitive `uuid@9.0.1` path through Firebase/Google Cloud packages. npm's complete remediation forces a breaking Firebase Admin downgrade, so no forced dependency change was made.
- Self-review: Deterministic tests probe double-send races, live/expired leases, retry state preservation, sentinel recalculation, DST duplicate/nonexistent times, quiet-hour rollover, authorization expiry, matching-index deletion, newer-index preservation, bounded/rotating cleanup, all three direct-client denies, exact provider hosts and private/unsupported URL rejection, non-followed redirects, fixed payload shape, aggregate log privacy, secret parameters, and dependency isolation. No endpoint, key, document ID, payload body, activity date, or secret value is logged.
- Remaining gates: Independent Task 6 review is pending. Firebase emulator/controlled-project checks, Blaze billing, real secrets, WAF proof, preview/deployment, and installed-device verification remain owner/Task 8 gates. No deployment or external configuration occurred.
- Commit target: `feat: schedule privacy-safe Web Push reminders`.

### 2026-07-17 — Task 6 security review remediation

- Role and scope: Task 6 implementation agent responding to the independent security rejection of `726b2f8`. Work remained limited to Task 5 subscription-generation compatibility and Task 6 scheduler/dispatcher hardening; Task 7, deployment, billing, secrets, WAF publication, live Firestore, and real push remained out of scope.
- Endpoint boundary: Registration and dispatch reject any percent encoding in the raw HTTPS authority before URL parsing. Dispatch canonicalizes an accepted endpoint once, requires WHATWG and legacy-parser hostname agreement, and passes only that canonical value to `web-push`. Exact provider hosts remain the only outbound targets.
- Shifted scheduling: The scheduler considers the immediately previous matching weekday only when quiet-hour shifting places its delivery on the current local date. Workout and follow-up retain the original activity date for deduplication, are delivered exactly once, and cannot resurrect a week-old event.
- Subscription races: New devices start at `subscriptionRevision: 1`. Active same-endpoint refresh atomically increments a valid revision and clears any old lease while preserving dispatcher-owned delivery state. Finalize, retry release, and terminal delete require the complete claimed identity: lease, subscription revision, raw stored endpoint, endpoint fingerprint, and both subscription keys. An old 201/204 or 404/410 response therefore cannot mutate a refreshed subscription.
- Failure semantics: Maintenance phases, individual cleanup records, due-record claims, lease releases, and aggregate logging fail-stop without aborting later due records. After an accepted push, finalization retries at most three times; if storage remains ambiguous, the dispatcher reports failure and deliberately retains the finite lease rather than immediately resending. The delivery contract is at-least-once: a process crash or persistently ambiguous finalize can leave a successfully delivered event unrecorded, and after the ten-minute lease expires a later invocation may send it again. Category/local-date state prevents repeats once finalization commits.
- Response policy: Only 404, 410, and reviewed 3xx responses remove the claimed subscription. 400, 401, 403, 413, 429, all 5xx responses, unknown responses, and network errors retain the record, release only a still-owned lease, and do not advance delivery state.
- Stored-state boundary: Before sending, the dispatcher validates canonical 32-byte document IDs and fingerprints, canonical on-curve P-256 and 16-byte auth keys, positive safe subscription revisions, endpoint shape/parser agreement, authorization/creation/update/next dates, paired lease state, and the complete scheduler state. Invalid records are quarantined with `enabled: false`, `nextNotificationAt: null`, and both lease fields cleared. Paused or intentionally all-category-disabled records are parked with a null next time without being treated as corrupt. Old endpoint indexes are healthy only when their document ID equals the current device fingerprint.
- Clock and cleanup: The scheduled export injects `() => new Date()`, and authorization, claim, lease, cleanup, and finalize operations read the clock afresh. Bounded maintenance continues to rotate healthy reviewed rows. Query and transaction failures contribute aggregate failure counts without exposing record data.
- TDD evidence: Task 5 revision RED failed 4/41 before the API/store changes and passed 41/41 after. Scheduler hardening RED failed 3/15 and passed 15/15 after the previous-day and complete-shape changes. Dispatcher hardening RED passed 19/31 with 12 expected failures before implementation; the expanded final dispatcher suite passed 34/34. Raw-authority API rejection was separately captured RED (`201` instead of `400`) and restored GREEN.
- Final verification: Focused Task 5/6 notification tests passed 90/90. Full `npm test` passed 317/317. Production Function syntax checks, the direct Functions dependency tree, and `git diff --check` passed. Root and Functions production audits still report the known `uuid@9.0.1` path: eight and seven moderate findings respectively, with no high or critical finding. Root remediation proposes a breaking Firebase Admin major update while Functions remediation proposes a breaking Firebase Admin downgrade, so neither forced change was applied. Independent re-review remains required before Task 6 is accepted or any deployment proceeds.
- Commit target: `fix: harden notification dispatch races`.

### 2026-07-17 — Task 6 approval and expiration consistency minor

- Review result: Independent re-review approved Task 6 with no Critical or Important finding. No Task 7 or deployment work was authorized by this closeout.
- Resolved reviewer minor: W3C `EpochTimeStamp` subscription expiration is integral. Task 5 registration previously accepted any finite nonnegative number while Task 6 dispatch required null or a nonnegative safe integer. Both layers now accept exactly `null`, zero, and other nonnegative safe integers, and reject negative, fractional, unsafe, and non-finite values.
- Deferred reviewer minor: An Admin-corrupted parked record with `nextNotificationAt: null` and no `updatedAt` is inert because it is not eligible for the due query, but the age-ordered maintenance query cannot discover it. Normal registration, preference, completion, lease, finalize, release, park, and quarantine writes always set `updatedAt`; broader recovery for externally corrupted/missing timestamps is deferred to final hardening rather than expanding this approved minor.
- TDD evidence: The cross-layer regression first failed because `validatePushSubscription` returned valid for `0.5`, while the dispatcher half already rejected and quarantined it. Replacing the API predicate with the dispatcher's nonnegative-safe-integer rule restored the targeted API/dispatcher test to 2/2. The focused Task 5/6 suite passed 92/92 and full `npm test` passed 319/319.
- Files and scope: `lib/notification-validation.js`, `test/notification-api.test.js`, `test/notification-dispatcher.test.js`, and this worklog only. No scheduled Function behavior, client, service worker, Task 7, dependency, secret, billing, Firebase, preview, or deployment change was made.
- Commit target: `fix: align push expiration validation`.

### 2026-07-17 — Task 7: installed-PWA notification controls and service worker

- Role and bounded scope: Task 7 implementation agent. Added only the installed-iPhone/Android PWA notification client, post-plan offer, Account preference controls, safe service-worker push/click behavior, and completion-date-only activity sync. Existing Task 4 preference validation and Task 5/6 server interfaces were reused. No dependency, billing, secret, WAF, Firebase, preview, production, or real-push action occurred.
- Opt-in and capability boundary: The one-time offer appears only after a genuinely new live plan succeeds; fallback, restored, edited, adapted, or profile-switched plans do not trigger it. The browser permission request exists only inside the explicit Enable action. Registration first checks same-origin server configuration and a valid VAPID key, then supports only secure installed iPhone Home Screen and Android PWAs with service workers, Notifications, and PushManager. Unsupported and denied states provide recovery guidance without repeated prompts.
- Client and cleanup behavior: The client reuses or creates a standards-based browser subscription, POSTs only the subscription plus normalized preferences, and persists only the returned opaque token and exact normalized preferences after success. All browser/API promise failures become safe client error enums. A failed new registration unsubscribes the new browser object. If the server issued a token but local persistence fails, cleanup best-effort DELETEs the server record before browser unsubscribe. Delete calls the server first, then local unsubscribe. An unconfirmed server deletion retains the token/preferences and presents a retry path; confirmed server deletion clears them even if browser unsubscribe cleanup is incomplete.
- User controls and accessibility: Results show an inline editable schedule, quiet hours, and the four notification categories beside the first-workout action. Account reuses the editor for Save, Pause/Resume, and Delete. Status/error regions are announced, async states disable controls and always clear `aria-busy`, long time-zone labels wrap, mobile controls collapse to one column, and interactive targets are at least 44 px.
- Service worker and activity privacy: Cache `spotterai-v35` includes the new modules. Push parsing rejects malformed or unbounded text, allow-lists category and same-origin Today destinations, uses the branded icon and fixed tag, and never opens a cross-origin payload URL. Notification clicks focus/navigate a same-origin window or open the canonical Today route with an allow-listed category. Page boot records only that category and removes the query while preserving the hash. Successful workout storage asynchronously PATCHes only the local `YYYY-MM-DD` completion date—never workout, exercise, duration, volume, difficulty, pain, profile, or intake data.
- Files: `notification-client.js`, `notification-ui.js`, `reminders.js`, `service-worker.js`, `index.html`, `style.css`, `workout-ui.js`, `app.js`, `test/notification-client.test.js`, `test/pwa.test.js`, `test/a11y.test.js`, `test/ui-copy.test.js`, `test/ui-layout.test.js`, and this worklog. `notifications.js` required no change because the approved Task 4 interfaces already met the client contract.
- TDD evidence: The initial focused RED passed 9/16 and failed the seven expected missing-client/UI/cache/push assertions. Focused RED/GREEN cycles then covered raw permission/service-worker/subscription failures, post-success control state, denial/save retries, malformed VAPID preflight, server cleanup after local persistence failure, async busy-state cleanup, and denial remaining disabled after action cleanup. The required focused suite passes 70/70.
- Automated verification: `node --test test/notification-client.test.js test/notifications.test.js test/pwa.test.js test/a11y.test.js test/ui-copy.test.js test/ui-layout.test.js` passes 70/70. `npm test` passes 348/348. Production module syntax checks and `git diff --check` pass.
- Visual evidence limitation: A local test server was available, but the current Browser tooling reported that no browser backend was available, so no 390×844 or 1440×900 screenshot evidence is claimed. Automated layout guards cover one-column mobile schedule/category/quiet-hour/actions, 44 px targets, shrink-safe rows, and long time-zone wrapping. Task 8/owner-controlled browser and physical-device verification remains required.
- Self-review: Fixed a cleanup-boundary server-record leak, remapped every raw browser rejection to safe enums, made successful Enable idempotently disabled, restored controls after recoverable errors, cleared Account busy state on load/delete, and prevented the Enable control from becoming usable again after permission denial. No token, endpoint, subscription key, response body, health input, or workout detail is logged or emitted as analytics.
- Independent review: Pending. No deployment or production readiness claim is made by this entry.
- Commit target: `feat: enable installed-PWA notification controls`.

### 2026-07-17 — Task 7 independent-review remediation

- Role and review state: Task 7 implementation agent responding to the initial independent review. The reviewer found six Important issues: empty schedules were accepted; the post-plan offer marker was consumed before the offer could be actionable and Account lacked a pre-permission recovery path; VAPID validation accepted malformed/off-curve keys; workout completion sync did not use the stored workout date at every creation call site; stored notification time zones were not migrated on boot/resume; and `notification_prompted` analytics were emitted before the client actually invoked the browser prompt. A second review found three further Important lifecycle/race issues and two Minors; the final re-review approved the remediated task with zero Critical and zero Important findings.
- Schedule boundary: preference validation now requires one to seven unique weekday/time entries, so empty schedules fail in the domain, client preflight, API, Account UI, and dispatcher scheduler rather than silently creating an enabled device with no reminders.
- Offer lifecycle and Account recovery: a new live-plan event displays the contextual offer only when the current configuration contains a WebCrypto-validated VAPID key and installed-device capability is supported. Rendering or failed preflight does not consume it. The marker is written only immediately before an actual browser permission prompt, or after successful subscription when permission was already granted; a denial therefore records that the user was genuinely asked without causing an automatic re-prompt. Unsupported/disabled/preflight-failed states keep the offer recoverable. Account exposes an expanded schedule and deliberate Enable action only when a current or restored plan exists; every general plan/profile event rerenders that gate, and the click path rechecks it. A confirmed server deletion clears the marker; an unconfirmed deletion retains the marker, token, and preferences for an explicit retry.
- VAPID preflight and analytics timing: the public key must be canonical unpadded base64url for exactly one uncompressed 65-byte P-256 point, and WebCrypto must successfully import it on P-256 before permission can be requested. The optional prompt callback runs immediately before `Notification.requestPermission()` only after the fresh configuration GET and every capability/key check; the UI emits `notification_prompted` from that callback. Disabled/raced configuration, invalid/off-curve keys, already-granted permission, and prior denial do not emit the event.
- Completion-date boundary: both full workout save and quick log use the exact `workout.date` returned by successful storage, then asynchronously PATCH only `{lastWorkoutCompletionDate}`. Edit/update and demo-seeding paths do not sync, and workout details remain excluded.
- Time-zone migration and mutation serialization: boot and visible resume use a single-flight availability refresh. A subscribed device whose saved preference zone differs from `Intl.DateTimeFormat().resolvedOptions().timeZone` PATCHes the existing normalized preferences with only the zone changed, persists the result, and rerenders. Same-zone and unsubscribed paths are no-ops. Availability/migration, Enable, Save/Pause, and Delete share one Account mutation lock, so an older migration cannot overwrite newer preferences or recreate a deleted subscription. Both Account and a visible offer remain disabled across an in-flight refresh; failures restore capability-aware controls and allow a later retry.
- Service-worker behavior coverage: executable mocked-worker tests prove malformed JSON fallback, bounded title/body, category and URL allow-lists, fixed icon/tag/renotify/data, same-origin focus/navigation, and canonical open-window fallback. Task 8 additionally changed installation to atomically precache the complete essential shell before `skipWaiting()`, preventing a partial offline cache from replacing the working service worker.
- TDD evidence: the first combined remediation RED run passed 151/171 and failed 20 expected assertions across schedule validation, offer/Account lifecycle, VAPID preflight, prompt timing, completion-date call sites, and time-zone migration. The second lifecycle remediation began from 28/38 with ten failures covering stale plan state, mutation races, marker timing, configuration actionability, recovery disclosure, and event binding. The final focused command (`node --test test/notification-client.test.js test/notification-ui-lifecycle.test.js test/service-worker-behavior.test.js test/notifications.test.js test/notification-api.test.js test/notification-schedule.test.js test/notification-dispatcher.test.js test/pwa.test.js test/a11y.test.js test/ui-copy.test.js test/ui-layout.test.js`) passes 180/180. Full `npm test` passes 384/384. Six production-module syntax checks and the combined `git diff --check 3c10289` pass.
- Files changed by remediation: `index.html`, `notification-client.js`, `notification-ui.js`, `notifications.js`, `quick-log.js`, `workout-ui.js`, `test/notification-api.test.js`, `test/notification-client.test.js`, `test/notification-schedule.test.js`, `test/notification-ui-lifecycle.test.js`, `test/notifications.test.js`, `test/service-worker-behavior.test.js`, `test/ui-copy.test.js`, and this worklog; the local SDD report/progress records are also updated.
- Independent review outcome: final re-review approved Task 7 with zero Critical and zero Important findings. Its two Minors were (1) a visible offer could remain clickable during an availability refresh and receive a blocked result without feedback, and (2) this worklog still described the previous lifecycle/count. The shared busy lock now covers both Account and the offer, including rerenders during an in-flight operation; the focused suite remains 180/180. This entry resolves the documentation mismatch.
- Visual and external limits: No screenshot, physical-device, preview, real-push, Firebase, billing, secret, WAF, deployment, or production evidence is claimed. Those gates remain owner/Task 8 work. Task 7 is not committed yet; the prior Git write was quota-blocked, and the owner's subsequent explicit approval is now recorded for the next safe commit attempt.

### 2026-07-17 — Task 8 local documentation and dependency gates

- Role and bounded scope: Release integration agent. Updated only local documentation and ran read-only/reproducibility checks; no Vercel, Firebase, billing, WAF, secret, preview, production, or physical-device state was changed.
- Documentation result: README now describes Metric cm/kg and Imperial ft/in/lb optional behavior; AI recovery; Vercel `/funnel/<event>` pageview inspection; installed iPhone/Android PWA requirements; plan-contextual permission; user schedule/category/quiet-hour/Pause/Delete controls; exact Vercel notification environment-variable names without values; local VAPID generation and Firebase Secret Manager handling; Blaze/budget warning; Firestore/rules/index/function deployment; WAF/429 readiness; emergency scheduler pause/resume; five-minute scheduling tolerance; and honest absence of suspended-PWA timer guarantees or Live Activities. Outdated claims of local/on-open reminders, a dependency-free backend, no database/auth, and guaranteed $0 operation were removed.
- Documentation sources: operational language was checked against current official Vercel Web Analytics documentation, Firebase scheduled-function/pricing/Secret Manager documentation, Google Cloud Scheduler pause/resume documentation, and Apple standards-based Web Push/Home Screen documentation. Repository commands were additionally checked against the installed `web-push` CLI and the actual function export/region.
- Dependency reproducibility: `npm install` and `npm install --prefix functions` both completed as already up to date; neither dependency tree or lockfile changed.
- Secret hygiene: the plan-prescribed tracked-file scan initially matched only the literal scan expression documented inside the implementation plan. Repeating the same scan while excluding that self-referential plan line produced no matches. No secret value, service-account private key, VAPID private key, signed token, or push endpoint was inspected or written to this worklog.
- Dependency audit: live `npm audit --omit=dev --json` reports 8 moderate, 0 high, and 0 critical findings in the Vercel tree; the Functions audit reports 7 moderate, 0 high, and 0 critical. Both sets reduce to `uuid@9.0.1` through Firebase/Google Cloud clients. The advisory affects buffer-writing forms of UUID v3/v5/v6; inspection of every installed caller in `gaxios`, `google-gax`, and `teeny-request` found only `uuid.v4()` calls, and SpotterAI imports Firebase Admin app/Firestore subpaths rather than Cloud Storage behavior. npm's complete suggested remediation is a breaking downgrade to old Firebase Admin/Functions majors, so no forced downgrade was applied. This is a documented moderate transitive limitation, not an unresolved high/critical gate.
- Automated evidence at the exact-tree checkpoint: current `npm test` passes 427/427 and the release-focused suite passes 264/264. Production-module syntax and `git diff --check 1b94973` pass. `npm run eval` passes 23/23 expectations, catches all 13/13 risky plans, and produces zero safe-plan false positives; `npm run eval:nutrition` confirms 15/15 local reference-anchor cases. The tracked-file secret scan, excluding its literal documented expression, returns no matches.
- Local review gate: Complete. Mobile/PWA and security/privacy reviews are Approved with zero findings in their final scope, and the documentation-only specification recheck is Approved with zero Critical or Important findings. The runbook keeps production registration disabled while allowing registration only in an owner-authorized controlled preview after its Firebase and WAF prerequisites pass. The owner-authorized Vercel preview is the next gate.
- Remaining external gates: owner-authorized Vercel preview and runtime checks; published per-IP WAF rule plus separate POST/PATCH/DELETE 429 proof; Firebase emulator or controlled-project verification; Firebase Blaze/budget confirmation; production secrets and rules/index/function deployment; installed iPhone and Android real-push verification; and explicit exact-commit promotion approval. No external-completion claim is made.

### 2026-07-17 — Task 8 final-review remediation

- Role and bounded scope: Release integration and remediation agents. Addressed only findings from the initial three-way Task 8 review and reconciled the versioned plan/worklog. No Vercel deployment, Firebase project, billing, WAF, secret, physical device, or production state was changed.
- Initial findings: the specification review found that the notification client sent a DELETE request shape incompatible with the real API handler. The security/privacy review found same-endpoint authorization takeover through endpoint-only reuse, refresh of a record with a live dispatcher lease, and deletion that discarded its only retry credential after an unconfirmed server failure. The mobile/PWA review found missing onboarding selection/focus semantics and touch sizing, invalid unit conversion producing `NaN`, browser-centric denied-permission guidance on iPhone, and service-worker activation after partial precache failure. A follow-up specification pass found the documentation and old evidence counts had not yet been reconciled.
- Server/client security resolution: the client-to-real-core-handler DELETE path is executable-tested. Same-endpoint refresh requires an exact endpoint plus `p256dh` plus `auth` proof, rejects disabled records and live or malformed leases before quota or mutation, permits stale/no-lease refresh, and preserves dispatcher-owned fields. An unconfirmed server deletion retains the opaque token and preferences and shows a safe Account retry state; a final 401 also preserves the browser subscription proof needed for later exact-proof renewal. Only confirmed server deletion clears the local credential. Tokens, endpoints, keys, response bodies, workout details, health inputs, and photos remain excluded from logs and analytics.
- Mobile/PWA resolution: onboarding chips expose current selection with `aria-pressed`, unit rerenders return focus to the selected unit, and chips have a 44 px minimum target. Invalid height/weight text stays correctable across unit switches without producing `NaN`. Denied-permission recovery now gives distinct installed-iPhone Settings and Android Settings instructions and is included in the offline shell. Service-worker installation uses atomic essential-shell precaching and invokes `skipWaiting()` only after every asset succeeds, so a partial cache cannot replace the working app.
- Documentation and hygiene resolution: README labels iPad best-effort outside official Release 1 verification, the implementation plan reflects retryable deletion and atomic precaching, the tracked-secret scan excludes its own documented expression and returns no matches, and trailing whitespace is removed. This worklog records the findings, resolutions, current counts, and Firebase emulator/controlled-project gate.
- TDD and verification evidence: security remediation began with six expected focused failures, then passed 197/197; UX/PWA remediation began with expected failures for `NaN`, accessibility/touch behavior, guidance integration, and failed precaching, then passed its owned 67/67. Subsequent hardening is recorded below. The exact current full suite passes 427/427 and the release-focused selection passes 264/264; evaluator and nutrition evidence remains 23/23 expectations, 13/13 risky plans caught with zero false positives, and 15/15 nutrition anchors. Syntax, whitespace, and tracked-secret checks pass.
- Files: `lib/notification-store.js`, `notification-client.js`, `notification-ui.js`, `notification-guidance.js`, `onboarding-ui.js`, `measurements.js`, `service-worker.js`, `style.css`, their focused tests, the implementation plan, and this worklog. No dependency or lockfile changed.
- Review state and next action: the first post-code specification pass confirmed the remediated code behavior but correctly withheld approval until this documentation reconciliation. Fresh independent specification, security/privacy, and mobile/PWA verdicts remain required before commit, push, or preview.
- External gates unchanged: Vercel preview/runtime inspection; WAF publication and per-method 429 proof; Firebase emulator or controlled-project verification; Blaze/budget approval; production secrets/rules/index/function deployment; installed iPhone and Android real-push checks; exact-commit promotion approval; and production smoke/error checks.

### 2026-07-17 — Task 8 exact-tree hardening and final review

- Role and bounded scope: Release integration, measurement/PWA, notification-security, and independent review agents. Resolved every second-round user-path finding and recorded the exact reviewed tree. No Vercel deployment, Firebase project, billing, WAF, secret, physical-device, or production state was changed.
- Measurement and accessibility findings: review found that finite invalid values could be relabeled across units, rounded near-boundary invalid height/weight could become valid, and published valid imperial boundaries could become invalid in Metric. The final model converts finite values, carries a serialized correction marker keyed only to logical height/weight whenever the source was invalid, blocks marked weight from nutrition, clears only the affected marker on a real user edit, and clamps only source-valid published boundaries. The marker remains confined to the onboarding draft and never enters mapped plan inputs, analytics, logs, AI requests, or nutrition data. Metric/Imperial labels, cm/kg and ft/in/lb fields, focus restoration, `aria-pressed`, inline validation, and 44×44 px chip targets remain intact.
- Offline/PWA findings: the original atomic list did not contain the complete JavaScript boot graph. Cache `spotterai-v35` now atomically precaches all 61 independently derived local modules plus the shell/manifest/branded icons before `skipWaiting()`. Executable tests cover failed v35 installation while active v34 remains available, activation, offline navigation, every boot-module fetch, and exclusion of API/cross-origin traffic.
- Authorization lifecycle findings: 180-day tokens previously had no renewal path. The client now retains safe typed HTTP status, proactively renews within seven days of expiry on boot/visible resume, and on authenticated 401 performs at most one silent exact-existing-subscription POST followed by one retry. It never prompts or creates a new browser subscription. Old credentials/preferences are restored if renewed-token persistence fails. If deletion renewal fails, a final 401 preserves the exact browser subscription proof; a later retry can renew, delete, unsubscribe once, and clear state without looping or exposing response content.
- Lease and capacity findings: anonymous refresh still requires exact endpoint plus both subscription keys. Registration readiness now rejects a global daily cap above 10,000, below the scheduled cleanup capacity of 14,400 records/day. Store PATCH/completion/delete mutations are transactional and reject live or malformed delivery leases with a generic retryable 409; stale/no lease proceeds. Deterministic claim-versus-PATCH/delete tests prove no successful mutation response can precede the claimed outbound send.
- TDD evidence: measurement/PWA follow-ups passed 49/49 and valid-boundary polish passed 27/27 after their expected RED assertions. Notification lifecycle/cap/lease hardening passed 205/205 after 401 renewal, cleanup-capacity, live-lease mutation, storage rollback, and two-attempt deletion-proof RED cases. Current `npm test` passes 427/427. The exact release-focused command across analytics, AI recovery, measurements/onboarding, notifications/API/auth/store/dispatcher/UI, accessibility/copy/layout, manifest, and executable service-worker behavior passes 264/264. Evaluator evidence is 23/23 expectations, all 13/13 risky plans caught, zero safe false positives; nutrition anchors are 15/15. Syntax, dependency-tree, whitespace, and tracked-secret checks pass.
- Independent outcomes: final mobile/PWA review is Approved with zero Critical, Important, or Minor findings after a 92/92 focused run and 425/425 then-current full run. Final security/privacy review is Approved with zero Critical, Important, or Minor findings in the remediation scope after 189/189 focused and 426/426 full verification. The final specification review verified the exact implementation, privacy boundary, architecture preservation, dependency trees, and automated evidence. Its documentation pass found that the Web Push runbook circularly required real-device registration while registration remained disabled and that the design status label was stale. The runbook now explicitly keeps production registration off, sequences owner billing/secret approval before controlled Firebase verification, permits registration only in the prepared owner-authorized preview for WAF and real-device proof, and promotes only the verified commit/configuration; the design now records owner approval. The documentation-only recheck is Approved with zero Critical or Important findings after fresh 426/426 full, 263/263 exact release-focused, and 140/140 notification/runbook-focused verification.
- Known deferred Minor: an Admin-corrupted parked notification record with `nextNotificationAt: null` and no `updatedAt` is inert and cannot be discovered by the age-ordered maintenance query. All normal application writes populate `updatedAt`; this is not a user-path regression. Recovery for externally corrupted/missing timestamps remains deferred.
- Files and dependencies: hardening changed measurement/onboarding, service-worker/cache, notification client/UI/API/store/config validation, and focused tests; README, the implementation plan, and this worklog reflect the behavior. No framework, hosting, package version, dependency tree, or lockfile changed.
- Remaining external gates: create and verify the owner-authorized Vercel preview and early runtime errors; publish the fixed-window per-IP WAF rule and prove separate POST/PATCH/DELETE 429 behavior; run Firebase emulator or controlled-project rules/indexes/registration/dispatch/cleanup/scheduler checks; record Blaze/budget approval and configure production secrets; deploy production rules/indexes/function with identifiers; verify installed iPhone and Android allow/deny, schedule, background receipt, tap, completion suppression/recovery, quiet hours/cap, pause/edit/delete, icon, and offline flows; approve exact-commit promotion; then run production smoke and early-error checks.

### 2026-07-17 — Owner-authorized preview verification and meal-photo follow-up

- Preview identity and scope: Git push of `codex/release-1` created non-production Vercel deployment `dpl_6J9R8BRouXZpkWZNUK6BxxeTa4fa` (`spotterai-git-codex-release-1-rshaw06.vercel.app`) from exact commit `8f02b6bab0c98326f6f4e3b937ee900948cec393`. It reached `READY` with no alias error. Production was not promoted or changed.
- Static and API baseline: live GET checks returned HTTP 200 for the app shell, manifest, `service-worker.js`, and notification client. The manifest exposes standalone display plus branded 192 px, 512 px, maskable, and Apple-touch assets. Service worker `spotterai-v35` exposes the complete 61-module atomic offline shell. `GET /api/notifications` returned `{"enabled":false}` with `no-store`, proving registration remained fail-closed in Preview.
- Browser flow evidence: the deployed landing page rendered meaningful content with no error overlay or browser console warnings/errors. The single guided plan flow opened from the hero CTA; Metric rendered cm/kg fields and Imperial rendered separate ft/in plus lb fields. Non-personal synthetic values advanced through goal, About you, schedule, safety acknowledgement, and preferences. Live Gemini generation returned `Conservative Beginner Hypertrophy 3x`, the deterministic audit rendered one issue to review, and `Start my first workout` routed to `#/dashboard` with day-one exercises prefilled. A non-personal Coach prompt returned a plan-aware answer. The notification schedule was prefilled from the plan but correctly unavailable outside an installed eligible PWA while registration was disabled.
- Meal-photo success evidence: a synthetic, non-personal image reproducing the previously reported lamb-chops-and-mashed-potatoes case was uploaded through the real preview UI. The deployed estimator identified `Lamb Chops and Mashed Potatoes`, returned an editable 566 kcal estimate with a 520–650 kcal range, protein/carbs/fat fields, one serving, and Dinner selected. No photo or raw AI response was stored in this worklog.
- Preview finding: a deliberate non-food upload of the SpotterAI icon produced the server's documented zero-valued `No food detected` sentinel, but the client treated it as a successful meal and left `Add to diary` enabled. The preview was therefore not approved as the release candidate. Root-cause tracing showed that `estimateMealPhoto` validated only completeness and nonnegative numbers, so the server sentinel crossed the client boundary as a normal success.
- Test-first resolution: a focused client test first failed with `Missing expected rejection` for a complete zero-valued photo response. `estimateMealPhoto` now requires at least one positive nutrition value after shape validation, classifies a zero-valued sentinel as `invalid_response`, and therefore reuses the existing provider-agnostic manual-entry plus `Try this photo again` recovery path while retaining the selected file for retry. The focused AI/analytics/UI selection passes 39/39, current full `npm test` passes 427/427, exact release-focused verification passes 264/264, syntax passes, and diff whitespace validation passes.
- Independent follow-up: the narrow post-preview review is Approved with zero Critical, Important, or Minor findings. It confirmed that only photo estimates use the all-zero rejection; all-zero text estimates remain accepted, a positive 2 kcal photo estimate remains accepted, and only the documented all-zero sentinel reaches the existing retry/manual recovery path. Fresh evidence was 39/39 AI/analytics/UI, 264/264 exact release-focused, and 427/427 full tests, plus passing syntax and diff checks.
- Next action: commit and push the approved fix, verify a replacement exact-commit preview, and re-run both the successful lamb-chops case and the non-food retry state. Firebase, WAF, installed-device, production-secret, promotion, and production-smoke gates remain unchanged and incomplete.

### 2026-07-17 — Replacement exact-commit preview approval

- Preview identity and authorization: after the owner's explicit commit, push, and preview approval, Vercel built non-production deployment `dpl_AxgjNKmvxNtZu7pp5dCfsFYCGtUK` at `spotterai-evftnojfg-rshaw06.vercel.app` from exact branch commit `bfacd177e2fcb7fdcb8f278ba039e7350b430d4f` (`Fix non-food meal photo recovery`). Vercel reported source `git`, state `READY`, the expected `codex/release-1` branch alias, and no alias error. Production was not promoted or changed.
- Live application baseline: the exact unique deployment returned HTTP 200 for the app shell, manifest, service worker, and `ai.js`. The deployed client contains the reviewed positive-nutrition photo guard; the manifest remains standalone with branded icons; the service worker remains `spotterai-v35` with the notification client in the offline graph. `GET /api/notifications` returned HTTP 200, `{"enabled":false}`, and `no-store`, so anonymous notification registration stayed fail-closed.
- Positive meal-photo recheck: the same synthetic, non-personal lamb-chops-and-mashed-potatoes fixture succeeded again through the replacement preview UI, producing an editable 566 kcal estimate, 520–650 kcal range, macro fields, and Dinner selection. The fixture, photo bytes, and raw model response are not stored in this repository or worklog.
- Non-food recovery recheck: uploading the branded SpotterAI icon through the real file chooser reached the provider-neutral recovery state: “The coach returned an incomplete result,” editable manual calories/macros/meal fields, and `Try this photo again`. `Add to diary` was absent, so the all-zero sentinel could not be saved as a successful meal. This directly verifies the preview-blocking edge case on the exact replacement commit.
- Browser and runtime evidence: the replacement preview rendered the recovery with no browser console warnings or errors. Vercel runtime queries scoped to this deployment found no 5xx logs and no warning, error, or fatal entries; observed dynamic requests were successful. No endpoint, image, prompt, response body, health input, token, or secret was copied into the worklog.
- Automated and independent evidence carried by the exact code commit: full `npm test` passes 427/427, the exact Release 1 focused suite passes 264/264, AI/analytics/UI focus passes 39/39, evaluator expectations pass 23/23 with 13/13 risky plans caught and zero false positives, nutrition anchors pass 15/15, and syntax/diff/secret checks pass. The narrow post-preview review approved the photo validation fix with zero Critical, Important, or Minor findings.
- Gate outcome: the application-level Vercel preview gate is complete. The notification backend remains deliberately disabled until the separately documented Firebase controlled-project/emulator checks, Blaze/budget approval, production secret setup, published WAF rule and per-method 429 proof, and owner-controlled installed-iPhone/Android notification matrix are complete. Exact-commit production promotion and post-promotion smoke/error checks also remain pending.
- Next action: commit and push this evidence-only worklog update, verify the resulting documentation-only preview reaches READY with a basic shell/API smoke check, then proceed to the external Firebase/WAF/device gates without enabling production registration.

### 2026-07-17 — Task 8 controlled Firestore emulator verification

- Role and bounded scope: Controlled-Firebase verification implementer, starting from release commit `2fe77584f786419ac97e42a3a97bd0b4eae66e04`. Added only the repository-owned local Firestore emulator gate and its exact evidence. No external Firebase project, billing, secret manager, WAF, Vercel environment, deployment, real push service, installed device, or production state was read or changed.
- Gate design: `npm run test:firebase-emulator` performs a generic Java preflight, then uses project-local `firebase-tools` with the fixed synthetic project `demo-spotterai-release-1`. `firebase emulators:exec` loads the checked-in Firestore rules/index configuration, starts only Firestore on loopback, runs the dedicated integration file, and shuts the emulator down. Missing Java fails distinctly with exit 2 and `FIREBASE_EMULATOR_PREREQUISITE_MISSING`; the gate never silently skips. Ordinary `npm test` does not discover the integration file.
- Real-emulator result: Firestore Emulator v1.21.0 passed 5/5 checks with zero failures and zero skips. Rules-unit-testing clients proved authenticated owner-only `users/{uid}` access and denied client reads/writes for `notificationDevices`, `notificationEndpointIndex`, and `notificationRegistrationCounters`. The real notification handler and `createNotificationStore` transaction registered a device, endpoint index, and UTC-day counter. The real dispatcher queried and claimed the due record, called only an in-process stub sender, finalized delivery state, removed an expired device and its matching index, removed an old counter, and retained the current counter.
- TDD evidence: Before implementation, direct execution of the dedicated test passed 0/1 and failed the intended local-emulator assertion. The emulator-configuration contract then passed 0/1 and failed because `firebase.json` lacked the required loopback emulator block. After implementation, the ordinary machine environment produced the required explicit Java-prerequisite exit 2; a temporary repository-external Java 21 runtime enabled the complete 5/5 emulator GREEN.
- Automated verification: Focused notification auth/API/store/schedule/dispatcher tests pass 106/106. Ordinary `npm test` passes 427/427 without starting or skipping the emulator gate. `npm audit --omit=dev` remains at the known 8 moderate, 0 high, and 0 critical Firebase Admin/Google Cloud findings. The complete tree reports 11 moderate, 0 high, and 0 critical findings; the additional development-only path is through the local Firebase CLI and OpenTelemetry/PubSub. No forced breaking remediation was applied.
- Privacy and safety boundaries: All project IDs and fixtures are synthetic. The readiness-only RSA key is generated ephemerally in the test process; fixed token/dedup/key fixtures are non-sensitive and never authorize an external service. `FIRESTORE_EMULATOR_HOST` must be loopback, both Google project variables must equal the fixed demo project, and the CLI states that non-emulated access for the demo project fails. The push sender is a local stub. Emulator debug logs are ignored. `NOTIFICATION_REGISTRATION_ENABLED` remains default-off outside the injected integration-test environment.
- Version-controlled files: `.gitignore`, `firebase.json`, `package.json`, `package-lock.json`, `scripts/run-firebase-emulator-gate.mjs`, `integration/firebase-emulator.mjs`, `test/notification-dispatcher.test.js`, and this worklog. The required detailed agent report remains local under `.superpowers/sdd/`.
- Remaining gates and concern: A working Java runtime is required to run this repository gate. The emulator executes the declared compound query and loads the checked-in index configuration, but it does not prove a production index deployment. Blaze/budget approval, production secret configuration, production rules/index/function deployment identifiers, WAF publication and per-method 429 evidence, real push, installed-device verification, exact-commit promotion, and production smoke checks remain incomplete and owner-gated.

## Required worklog entry format

Each implementation, review, verification, or deployment entry must record:

- Date and agent role.
- Bounded task and acceptance criteria.
- Result and material decisions.
- Files and commit identifiers.
- Automated tests and their outcomes.
- Manual or preview evidence when relevant.
- Review findings and their resolution.
- Blockers, owner approvals, and exact next action.

## Release sign-off

Release sign-off remains empty until all gates pass. Final sign-off must include the release commit, Vercel deployment identifier, Firebase function deployment identifier when enabled, complete automated test results, real-device notification evidence, unresolved known limitations, and the owner's production-promotion approval.
